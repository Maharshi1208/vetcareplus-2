# backend/.env.example 
PORT=4000
NODE_ENV=development
DATABASE_URL=postgresql://vc_user:vc_pass@localhost:5433/vetcare_db?schema=public
CORS_ORIGIN=http://localhost:5173

# Auth
JWT_SECRET=dev_super_secret_change_me_at_least_32_chars
JWT_EXPIRES=1d
BCRYPT_ROUNDS=10

# SMTP / MailHog
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_FROM=no-reply@vetcare.local
